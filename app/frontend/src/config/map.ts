/**
 * Map Configuration
 *
 * Shared constants for all Leaflet map components.
 */

/** Default map center (UK survey area) */
export const DEFAULT_MAP_CENTER: [number, number] = [51.159480, -2.385541];

/** Default zoom level */
export const DEFAULT_MAP_ZOOM = 13;
