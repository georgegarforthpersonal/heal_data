version: '3.8'

services:
  streamlit:
    build: .
    container_name: heal_butterflies_app
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - ./app:/app/app
    restart: unless-stopped